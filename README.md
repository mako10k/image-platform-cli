# image-platform CLI

Independent command-line client for the image platform.

The repository currently contains the candidate authentication contract only. No executable CLI,
WorkOS setting change, invitation, live request, or credential has been created here.

See [`docs/auth-contract.md`](docs/auth-contract.md).

